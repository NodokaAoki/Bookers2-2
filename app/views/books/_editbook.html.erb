<h2>New book</h2>

<%= form_for(book) do |f| %>

Title<br>
<%= f.text_field :title, class: "field" %><br>

Opinion<br>
<%= f.text_area :body, class: "field" %><br>

<%= f.submit 'Create Book', class: 'btn btn-primary btn-create' %>

<% end %>